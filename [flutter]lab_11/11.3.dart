// 11.3 -  W.A.P. to create list of images path in List and display it using GridView and then GridView.builder. (B)

import 'package:flutter/material.dart';

class hi extends StatefulWidget {
  const hi({super.key});

  @override
  State<hi> createState() => _hiState();
}

class _hiState extends State<hi> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
    body: Center(
      child: Row(
        children: [
          
        ],
      ),
    )
      );
  }
}
